Run locally:
1) python3 -m pip install --user --break-system-packages -r requirements.txt
2) uvicorn app:app --host 0.0.0.0 --port 8766 --app-dir /home/node/.openclaw/workspace-user1/apps/finance-hub
Then open http://NAS_IP:8766
