# 2026-03-22 日志

## 今日主要工作

### Media Hub 今晚优化（第五轮）
- 去掉了推荐页"第20部"文案，改为只显示更新时间
- 手机端"今晚看什么"卡片封面：恢复满宽 + 3:4 比例，不再窄条
- 容器启动参数补全代理环境变量：HTTP_PROXY / HTTPS_PROXY / NO_PROXY
- 容器重建多次，最终容器 ID: d924f7fad9d6
- 当前部署模式：--network host，端口 8765

### 问题记录
- 机器为多网段环境（172.17.0.1 / 192.168.50.209 等），外部访问 8.218.9.10:8765 是否通需要佳奕确认
- 云服务器安全组可能是阻断原因
- 下次启动容器务必带代理环境变量：`-e HTTP_PROXY='http://192.168.50.209:7890' -e HTTPS_PROXY='http://192.168.50.209:7890' -e NO_PROXY='...'`

### 待确认
- 佳奕是否能看到 http://8.218.9.10:8765/recommendations
- 今晚的封面布局优化（满宽3:4）是否顺眼

## 上午补充（11:00-11:28 UTC）

### 首页改造完成
- 首页移除 hero section 和 5 按钮条，改为：今日推荐大卡 → 2×2 统计 → 观影趋势图
- 统计卡片恢复 2×2 布局
- 当前容器：media-hub-test（latest hash: aae5c343ce5）

### 智能推荐流程（smart_recommend.py）
- 流程：读口味 → TMDB 拉候选 → 生成推荐语 → 写库
- `insert_recommendation` 已支持完整字段：subject_id, tmdb_id, title, kind, year, intro, douban_rating, cover_url, url, recommendation_note, status, recommended_at, recommend_rank, recommend_source
- **已知 Bug**：
  - douban_rating 被错误除以 10（7.6分变成 0.76），已修复代码但最新一条（河狸变身计划 0.76 分）仍是错的
  - 新增推荐偶发 url/tmdb_id 未写入，导致无法跳转
  - LLM 推荐语调用失败（Ollama 未装），fallback 到通用模板"值得一看的movie作品"
- 推荐数量：status='recommended' 共 24 条，dislike 3 条排除后可见 21 条

### OpenClaw 自身作为 LLM 的方案
- 方案：给 Media Hub 加 `/api/generate-note` 接口，heartbeat 时 OpenClaw 直接生成推荐语
- 尝试通过 host.docker.internal / 192.168.50.209:18789 调用 OpenClaw gateway API，但端点 404/502 均不通
- 改用模板生成 + plot 解析生成个性化推荐语（fallback 方案）
- `/api/generate-note` 接口已加到 app.py（需要修复同步/异步兼容问题）

### 待修复（最高优先）
1. 修正已写入的错误评分（0.76 → 7.6）
2. 补全最新推荐（河狸变身计划）的 url/tmdb_id
3. 修复 `/api/generate-note` 的同步/异步兼容问题，重建容器
4. 测试模板生成的推荐语是否比通用版更有区分度
5. 下次心跳时验证 OpenClaw 直接生成推荐语的完整流程

### HEARTBEAT.md 更新
- 新增推荐阈值：< 100 条时触发（原 20）
- 推荐语生成：直接用 OpenClaw 自身大模型（不用外部 API）
- 强制只写生产库 `/app/data/douban_media.db`
