
- 2026-03-21 Media Hub 推荐流持续重构：推荐条目正式落到主库 `douban_watch_history`，使用 `status='recommended'` + `tmdb_id/subject_id` 去重；状态流打通为 `recommended -> wish / collect`，并将推荐语持久到 `recommendation_note`。
- 2026-03-21 修复了推荐持久化关键问题：Docker 远端 daemon 不能直接 bind mount workspace 路径，改为使用持久卷 `media-hub-db` 与 `media-hub-covers`；容器统一读取 `/app/data/douban_media.db` 和 `/app/covers`，重启后推荐/已看状态不再回退。
- 2026-03-21 新增“不喜欢”机制：复用字段 `recommend_feedback='dislike'` 作为隐藏/排除标记；推荐页、首页、影视库、待看页、本地 discover 默认都隐藏 dislike 项；影视库状态筛选新增 `👎 不喜欢`，该视图仅显示被 dislike 的条目，并提供“改为待看”按钮用于清除 dislike 并恢复到 wish。
- 2026-03-21 用户明确偏好：Media Hub 推荐区按钮与首页/推荐页/列表卡片必须尽量统一；推荐按钮顺序偏好为 `TMDB -> 想看 -> 看过 -> 👎`；不需要“喜欢”、详情、编辑等多余按钮。
- 2026-03-21 用户明确偏好：影视库编辑弹窗要简洁，使用和推荐页一致的大星星评分器；移除封面 URL 手动输入与推荐语编辑；日期输入框必须对齐且不溢出。
- 2026-03-21 今天已完成多轮 Media Hub UI/状态流部署并多次 build/restart；相关 git 提交至少包括 `7c2afc9`、`56699e5`、`ff316bd`。
